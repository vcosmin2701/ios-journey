// Sets
// Duplicate items can't be added, and items are not stored in a particular order.
let people = Set(["Denzel Washington", "Tom Cruise", "Nicolas Cage", "Samuel L Jackson"])

print(people)

// Insert values into a set
var brands = Set<String>()
brands.insert("Apple")
brands.insert("Casio")

print(brands)
