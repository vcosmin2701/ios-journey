// var - can be changed over time
var greeting = "Hello, Cosmo"
greeting = "Hello again, Cosmo"

print(greeting)

// constant - can't be changed
let character = "Tom"
// character = "Jerry" -> this will cause an error, because is a 'let' constant

print(character)